#include <vm/vm.h>

#include <stddef.h>

struct object *allocObject(struct object *class)
{
	return NULL;
}

struct object *allocArray(struct object *class, int count , int size)
{
        return NULL;
}

struct object *allocTypeArray(int type, int size)
{
	return NULL;
}
